# Written by spool. Reproduced from spool.toml and spool.lock, so it is not
# source and does not belong in a commit.
twill_modules/
.spool/

# Checkpoints and logs a run writes.
runs/
*.ckpt
*.ckpt.best

# tests/checkpoint_test.tw writes a checkpoint into a directory it creates, to
# prove that writing one creates the directory.
tests/tmp_*/
